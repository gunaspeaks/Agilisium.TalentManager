@model Agilisium.TalentManager.Web.Models.SubPracticeModel

@{
    ViewBag.Title = "Update Sub-Practice";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="panel panel-default">
    <div class="panel-heading">
        <h3>Update Sub-Practice</h3>
    </div>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="panel-body">
            @Html.ValidationSummary(false, "", new { @class = "text-danger" })

            @foreach (var property in ViewData.ModelMetadata.Properties)
            {
                if (property.PropertyName != "SubPracticeID"
                    && property.PropertyName != "PracticeName"
                    && property.PropertyName != "IsReserved"
                    && property.PropertyName != "LoggedInUserName")
                {
                    <div class="form-group">
                        <label>@(property.DisplayName ?? property.PropertyName) </label>
                        @if (property.PropertyName == "PracticeID")
                        {
                            @Html.DropDownList("PracticeID", (IEnumerable<SelectListItem>)ViewBag.PracticeListItems, null, new { @class = "form-control" });
                        }
                        else
                        {
                            @Html.TextBox(property.PropertyName, null, new { @class = "form-control", rows = 2 })
                        }
                    </div>
                }
                else
                {
                    @Html.Hidden(property.PropertyName);
                }
            }
            <div class="panel-footer">
                <input type="submit" value="Save" class="btn btn-primary" />
                @Html.ActionLink("Cancel and Go Back", "List", null, new { @class = "btn btn-default" })
            </div>
        </div>
    }
</div>
